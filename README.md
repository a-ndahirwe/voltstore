# voltstore

## Admin Login

A default admin user is seeded automatically when the backend starts if it does not already exist.

- Email: `admin@voltstore.com`
- Password: `Admin123!`

You can sign in at `http://localhost:3000/signin` and then access the admin panel at `http://localhost:3000/admin`.
